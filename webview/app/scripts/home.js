/* jshint unused:false, devel:true */
var home = (function($, window, document) {
  'use strict';

  var home = {
    init: function() {
      console.log('test');
    }
  };

  return home;
}(jQuery, window, document));